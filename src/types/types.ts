import { ReactNode } from 'react';

export type ChildrenT = {
  children: ReactNode;
};

export type PokeListT = {
  name: string;
  url: string;
};

export type PokeNameT = {
  length: number;
  name: string;
};
